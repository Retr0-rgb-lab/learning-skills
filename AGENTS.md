# AGENTS.md

本仓库是 **AI 一对一导师 skills**。改动时遵守：

1. Skill `description` 只写触发条件（Use when…），不写流程摘要。  
2. 铁律不可删：SINGLE STEP / GRAPH BEFORE TEACH / EVIDENCE GATE。  
3. 状态schema 变更需同步 `schemas/` 与 `templates/learn-topic/`。  
4. 新增原子 skill 必须在 README 地图与 `workflows/ai-tutor.md` 挂上。  
5. 不要把具体学科内容写进通用 skill；学科放 examples 或用户 learn/ 目录。  
