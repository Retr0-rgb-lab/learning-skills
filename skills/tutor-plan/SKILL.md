---
name: tutor-plan
description: Use when writing Mermaid dependency graph after probing, learning roadmap in classroom file, prerequisite chain for tutoring.
---

# tutor-plan

- 探测结束后 **必须** 在 classroom 输出 Mermaid + 节点表  
- 节点含：状态、通过标准（你需要能做到）  
- 图要服务 Active quest；边稀、无环  
- 深教前无图 → 先补图（可极简）  
- 镜像可写 `_agent/dependency-graph.md`
